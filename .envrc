#!/bin/bash
if [ -f .env ]; then dotenv .env; fi
if [ -d .env ]; then
  for X in .env/*; do
    # shellcheck disable=SC1090
    case "$X" in
      *.env) dotenv "$X" ;;
      *.sh) source "$X" ;;
    esac
  done
fi
